import { Formbox } from '../../components/form/Formbox';
import './index.css';

export const Signup = () => {
    return (
        <div className="signup">
            <Formbox state="signup"></Formbox>
        </div>
    );
}